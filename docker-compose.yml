version: "3"
services:
  web:
    build: .
    command: gunicorn portfolio_project.wsgi --bind 0.0.0.0:8000
    container_name: web
    tty: true
    expose:
      - 8000
    volumes:
      - /static/:/code/portfolio/static/

  nginx:
    image: nginx:1.13
    container_name: nginx
    ports:
      - 80:80
    volumes:
      - ./nginx/conf:/etc/nginx/conf.d
      - /static/:/static/
      - /var/log/nginx/:/var/log/nginx/
    depends_on:
      - web